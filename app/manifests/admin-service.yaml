apiVersion: apps/v1
kind: Deployment
metadata:
  name: admin-service
  labels:
    app: admin-service
spec:
  imagePullSecrets:
    - name: regcred
  replicas: 1
  selector:
    matchLabels:
      app: admin-service
  template:
    metadata:
      labels:
        app: admin-service
    spec:
      containers:
        - name: admin-service
          image: nhqb3197/admin-service:1.0.0
          ports:
            - containerPort: 8000
          readinessProbe:
            httpGet:
              path: /admin/health
              port: 8000
            livenessProbe:
              httpGet:
                path: /admin/health
                port: 8000
---
apiVersion: v1
kind: Service
metadata:
  name: admin-service
spec:
  selector:
    app: admin-service
  ports:
    - port: 8000
      targetPort: 8000